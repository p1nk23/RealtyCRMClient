<Window x:Class="RealtyCRMClient.AnalyticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        xmlns:converters="clr-namespace:RealtyCRMClient.Converters"
        Title="Аналитика" Height="600" Width="800">
    <Window.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Выбор статуса -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Выберите статус:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox ItemsSource="{Binding Statuses}" 
                      SelectedItem="{Binding SelectedStatus}" 
                      Width="200" 
                      DisplayMemberPath="DisplayName"/>
            <Button Content="Обновить" 
                    Command="{Binding RefreshCommand}" 
                    Width="100" 
                    Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Таблица статистики -->
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding StatisticsData}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Тип" Binding="{Binding Type}" Width="100"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="150"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Count}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- График -->
        <lvc:CartesianChart Grid.Row="2" 
                            Series="{Binding ChartSeries}" 
                            XAxes="{Binding XAxes}" 
                            YAxes="{Binding YAxes}" 
                            Height="300"/>

        <!-- Индикатор загрузки -->
        <ProgressBar Grid.Row="3" 
                     IsIndeterminate="{Binding IsLoading}" 
                     Height="20" 
                     Margin="0,10,0,10" 
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <!-- Сообщение об ошибке -->
        <TextBlock Grid.Row="4" 
                   Text="{Binding ErrorMessage}" 
                   Foreground="Red" 
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"/>
    </Grid>
</Window>